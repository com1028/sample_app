<% provide(:title, 'All users') %>
<h1>ユーザー一覧</h1>
<p>user search</p>
<%= form_tag(users_path) do %>
  <%= text_field_tag :search %>
  <%= submit_tag 'Search', :name => nil %>
<% end %>
<%= will_paginate %>

<ul class="users">
  <%= render @users %>
</ul>

<%= will_paginate %>

<!-- 機能追加11/25-->
<% provide(:title, 'All users') %>
<h1>All users</h1>
<%= will_paginate %>

<!--  検索拡張機能  -->
